/**
 * User Logout Function
 *
 * This function handles user logout by redirecting to the logout route.
 * When called, it navigates the user to the "/logout" endpoint, effectively logging them out of the session.
 */

function logout() {
    window.location.href = "/logout";
}
